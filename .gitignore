# ── .NET / Azure Functions ────────────────────────────────────────────
**/bin/
**/obj/
# dotnet ef 的 Roslyn BuildHost 在 macOS 上會建出字面名為 `bin\Debug` 的目錄
# （Windows 風格路徑未轉換）。它不是 `bin/`，所以上面那條擋不住。
**/bin\\Debug/
**/temp/
.vs/

# Azure Functions local settings（含連線字串與金鑰 — 絕不進版控）
**/local.settings.json

# Azurite Storage Emulator
AzuriteConfig/
__azurite_db_*__.json
__blobstorage__/
__queuestorage__/
.azurite/
azurite-debug.log

# ── Next.js / Node ────────────────────────────────────────────────────
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.vercel/

# ── 工具與編輯器 ──────────────────────────────────────────────────────
.vscode/
.wrangler/
.DS_Store
Thumbs.db

# ── 本機產出 ──────────────────────────────────────────────────────────
output/

# 匯入用的中繼檔（由 scripts/export-content.mjs 與舊站稽核產生，可隨時重建）
artifacts/

# ── 客戶素材：僅同步至 NAS，不進 GitHub ───────────────────────────────
# 由 scripts/sync-nas-assets.sh 快照到 Remote_NAS 的 assets 分支。
# reference/ 約 260MB（字型 TTC、CIS Guide Book .ai/.pdf、官網圖片 zip），
# 且含客戶未公開素材，不可推上 public GitHub repo。
mockup/
reference/

# 版位照片由 scripts/sync-design-tokens.mjs 從 mockup 複製，屬客戶素材，同樣不進 GitHub。
# 正式站的圖片來自 CMS 的 Blob 媒體庫；沒有這些檔時前台會顯示虛線佔位框。
apps/web/public/assets/

# ── 後台 SPA 的建置產物（由 apps/admin build 產生，進 apps/web/public/admin）─────
apps/web/public/admin/
