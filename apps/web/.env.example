# 公開站的正式網址。canonical / hreflang / sitemap / OG 都以它為準，
# 非正式環境請填該環境自己的網址 —— robots.ts 以它判斷要不要開放索引。
NEXT_PUBLIC_SITE_URL=https://www.vicround.com

# Content API（fn-public）。伺服器端 SSR 用這一個，不會進 client bundle。
API_BASE=http://localhost:7071/api/v1

# 發布後由 fn-admin 呼叫 /api/revalidate 的共用密鑰（見 docs/cms.md「Publish → revalidate」）。
REVALIDATE_SECRET=change-me

# 前台會員區（/member/**、/account/**、/api/v1/account/**）的總開關。
# 預設關閉 —— 沒設或不是 true，入口不渲染、那些網址一律 404。要開就設成 true
# 再重新部署，不必改程式。用 NEXT_PUBLIC_ 前綴是因為 SWA 沒有執行期的應用程式設定，
# 值必須在 build 時內嵌（同 NEXT_PUBLIC_SITE_URL）。細節見 lib/features.ts。
NEXT_PUBLIC_MEMBERS_ENABLED=false
