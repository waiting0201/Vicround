# Admin API base。留空即用預設 `/api/admin`（與公開站同源，走 SWA 的 /api 代理）。
# 本機開發由 vite.config.ts 的 proxy 轉到 http://localhost:7071。
VITE_ADMIN_API_BASE=

# 後端（fn-admin）還沒開工，所以開發模式預設走 src/lib/mock.ts 的記憶體假資料，
# 讓 27 個畫面看得到東西。真的 API 起來之後設成 0 就會改打真的端點。
# 正式 build（vite build）一律不使用假資料，與這個設定無關。
VITE_ADMIN_MOCK=1
